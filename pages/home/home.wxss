.swiperClass {
  margin: 0;
}

.slide-image {
  width: 100%;
  height: 75%;
  position: relative;
  border-radius: 20rpx;
}

image.active {
  transform: none;
  transition: all 0.2s ease-in 0s;
}

image.quiet {
  transform: scale(0.90);
  transition: all 0.2s ease-in 0s;
}
